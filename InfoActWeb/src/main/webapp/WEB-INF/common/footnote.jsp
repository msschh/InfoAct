<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>

Version ${sessionScope.appInfo.majorVersion}.${sessionScope.appInfo.minorVersion}.${sessionScope.appInfo.release}
${sessionScope.appInfo.stage} (${sessionScope.appInfo.environment})
